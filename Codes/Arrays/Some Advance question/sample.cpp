#include <bits/stdc++.h>
#include <iostream>
using namespace std;

#include <vector>
typedef pair<int, int> pii;
typedef vector<int> vi;
#define pb push_back
#define MP make_pair
#define endl "\n"
#define boost                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)
#define F first
#define S second
const int inf = (int)1e18;
const int ninf = (int)-1e17;
#define rep(a, b) for (int a = 0; a < b; a++)
#define per(a, b) for (int a = b - 1; a >= 0; a--)
#define FOR(it, start, end) for (auto it = start; it != end; it++)

int main()
{
    boost;
    // int m[7] = {9, 7, 7, 9, 7, 7, 9};
    // int m[3] = {1, 2, 3};
    // vector<int> arr(m, m + 3);
    // cout << arr.size() - 3 << endl;
    // // for (size_t i = 0; i < count; i++)
    // // {
    // //     /* code */
    // // }

    // if (-1 >= (int(arr.size()) - 3))
    // {
    //     cout << " R";
    // }
    // else
    //     cout << "s";
    unsigned long int a = 0;
    if (-1 >= a)
    {
        cout << " yes";
    }
    else
        cout << "no";

    return 0;
}